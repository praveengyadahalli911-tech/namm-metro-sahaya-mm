import 'package:flutter/material.dart';

/// Premium Design System Colors (v2.0 - High Fidelity)
/// Inspired by modern glassmorphic and dark-themed aesthetics.
class AppColors {
  // ── Core Neutrals ──────────────────────────────────────────────────────────
  static const background = Color(0xFF0D1117);
  static const surface = Color(0xFF161B22); // Card background
  static const surfaceLighter = Color(0xFF21262D);
  static const border = Color(0xFF30363D);

  // ── Brand Colors ───────────────────────────────────────────────────────────
  static const primary = Color(0xFF7B2FBE); // Vibrant Purple
  static const primaryDark = Color(0xFF4A148C);
  static const accent = Color(0xFF00E676); // Glowing Green
  static const accentBlue = Color(0xFF1565C0);
  
  // ── Status Colors ──────────────────────────────────────────────────────────
  static const warning = Color(0xFFE65100);
  static const error = Color(0xFFC62828);
  static const success = Color(0xFF2E7D32);

  // ── Typography ─────────────────────────────────────────────────────────────
  static const textPrimary = Color(0xFFE6EDF3);
  static const textSecondary = Color(0xFF8B949E);
  static const textDim = Color(0xFF484F58);

  // ── Special ───────────────────────────────────────────────────────────────
  static const glassOverlay = Color(0x33FFFFFF);
  static const glowPurple = Color(0x667B2FBE);
  static const glowGreen = Color(0x6600E676);
  
  // ── Line Colors ──────────────────────────────────────────────────────────
  static const purpleLine = Color(0xFF7B2FBE);
  static const greenLine = Color(0xFF009033);
  static const lightPurpleBg = Color(0xFFF3E5F5); // Very light purple for badges/highlights
}
